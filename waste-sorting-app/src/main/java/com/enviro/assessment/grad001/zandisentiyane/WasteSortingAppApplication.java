package com.enviro.assessment.grad001.zandisentiyane;

import org.springframework.boot.SpringApplication;
import org.springframework.boot.autoconfigure.SpringBootApplication;

@SpringBootApplication
public class WasteSortingAppApplication {

	// The main method serves as the entry point to run the Spring Boot application.
	public static void main(String[] args) {
		// Runs the Spring Boot application, initializing the application context and
		// starting the embedded server.
		SpringApplication.run(WasteSortingAppApplication.class, args);
	}

}
